class CreateInvitations < ActiveRecord::Migration
  # commented out - don't have time to figure out what Jon was working on here
  #  def self.up
  #create_table :invitations do |t|
    #    t.column :user_id, :integer # who sent the invitation
    #  t.column :sent_to, :string # email address of friend
    #  t.column :message, :text # a custom message
    #  t.column :created_on, :datetime # when it was sent
    #  t.column :bounced_on, :datetime # used only if message bounced
    #    end
    #    rename_column :users, :referrer, :url
    #add_column :users, :referrer_id, :integer
    #add_column :users, :invitation_count, :integer
    ## Move data from the old referral tables into the new column
    #refs = Referral.find(:all)
    #refs.each do |r|
      #  u = User.find(r.referred_id)
      #u.referrer_id = r.referrer_id
      #end
      # end

      #  def self.down
      # drop_table :invitations
      #remove_column :users, :referrer_id
      # remove_column :users, :invitation_count
      #rename_column :users, :url, :referrer
      #end
end
