module Admin::EmailManagementHelper
  #def 
  #MemberEmailStat.count_by_sql(['select count(distinct user_id) from member_email_stats where email_id = ?', 1])
  #NewsletterEmailStat.count_by_sql(['select count(distinct subscriber_id) from newsletter_email_stats and email_id = ?', 2])
end
